# API Key para autenticação (OBRIGATÓRIO)
API_KEY=sua-api-key-aqui

# Configuração do banco de dados de pesquisas (OBRIGATÓRIO)
DB_PESQUISAS_URL=postgresql://usuario:senha@host:porta/banco

# Configuração do banco de dados de clientes (OBRIGATÓRIO)
DB_CLIENTES_URL=postgresql://usuario:senha@host:porta/banco

# Porta do servidor
PORT=3000

# Ambiente (development, production)
NODE_ENV=development

# Nível de log (error, warn, info, debug)
LOG_LEVEL=info
